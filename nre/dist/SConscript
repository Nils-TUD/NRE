# -*- Mode: Python -*-

Import('env')
import os

# make a link to imgs to prevent a copy
env.Command(
	Dir(env['BUILDDIR'] + '/dist/imgs'),
	Dir('#dist/imgs'),
	'ln -sf `readlink -f $SOURCE` $TARGET'
)
