# -*- Mode: Python -*-

Import('env')

ccsrc = [Glob('*.cc'), Glob('*/*.cc'), Glob('*/*/*.cc')]
ssrc = [Glob('*.s'), Glob('*/*.s'), Glob('*/*/*.s')]
lib = env.StaticLibrary('libstdc++', ccsrc + ssrc)
env.Install(env['LIBPATH'], lib)
